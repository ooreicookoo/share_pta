<%= form_with(model: [report, report_comment]) do |form| %>
  <% if report_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(report_comment.errors.count, "error") %> prohibited this comment from being saved:</h2>
      <ul>
      <% report_comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= form.label :コメント, id: "reports-coment-form__comment_title" %>
    <%= form.text_area :comment_content, id: "reports-comment-form__comment" %>
  </div>
  <div class="actions">
    <%= form.submit  id:"reports-coment-form__comment_submit" %>
  </div>
<% end %>
